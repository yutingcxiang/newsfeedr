import React from 'react';
import ArticleCard from './ArticleCard'

const ArticleList = () => {
  // const articles = this.props.articles.map(article => <ArticleCard />)

  return(
    <div>
    ArticleList
    </div>
  )
}

export default ArticleList
